<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.file">
	<select id="selectArticleFiles" resultType="java.lang.String" parameterType="java.lang.String">
		select filename
		from yomul_files
		where article_no = #{no}
		order by no asc
	</select>
	
	<!-- 파일 업로드 -->
	<insert id="uploadfile" parameterType="com.yomul.vo.FileVO">
		INSERT INTO YOMUL_FILES 
		(
			  ARTICLE_NO
			, NO
			, FILENAME
		) 
		VALUES
		(
			  #{article_no}
			, YOMUL_FILES_NO_SEQ.NEXTVAL
			, #{filename}
		)
	</insert>
	
	<!-- 다음 파일 번호 가져오기 -->
	<select id="getnextfileno" resultType="_int">
		SELECT YOMUL_FILES_NO_SEQ.NEXTVAL 
		FROM DUAL
	</select>
</mapper>