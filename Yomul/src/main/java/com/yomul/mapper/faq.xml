<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.faq">
	<!-- 관리자 FAQ 글쓰기 데이터 저장 -->
	<insert id="getAdminFaqWrite" parameterType="com.yomul.vo.FaqVO">
		INSERT
		INTO 
		YOMUL_FAQ_ARTICLES
		(
			NO
			, CATEGORY
			, WRITER
			, TITLE
			, CONTENT
		)
		VALUES
		(
			'FA'||YOMUL_FAQ_ARTICLES_NO_SEQ.NEXTVAL
			, #{categoryNo}
			, #{writer}
			, #{title}
			, #{content}
		)
	</insert>
	
	<!-- 관리자 FAQ 목록 가져오기 -->
	<select id="getAdminFaqList" resultType="com.yomul.vo.FaqVO">
		SELECT 
		* 
		FROM 
		V_Y_FAQ_ARTICLES 
		ORDER BY 
		NO 
		DESC
	</select>
	
</mapper>