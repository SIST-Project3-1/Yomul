<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.notice">

	<!-- 공지사항 작성 -->
	<insert id="writeNotice" parameterType="com.yomul.vo.NoticeVO">
		INSERT INTO YOMUL_NOTICES
		(
			  NO
			, WRITER
			, TITLE
			, CONTENT
		)
		VALUES
		(
			  'N'||YOMUL_NOTICES_NO_SEQ.NEXTVAL
			, #{writer}
			, #{title}
			, #{content}
		)
	</insert>
	
	<!-- 공지사항 수정 -->
	<update id="updateNotice" parameterType="com.yomul.vo.NoticeVO">
		UPDATE YOMUL_NOTICES
		SET
			WRITER=#{writer}
			, TITLE=#{title}
			, CONTENT=#{content}
		
		WHERE
			  NO=#{no}
		
	</update>
		
	<!-- 공지사항 삭제 -->
	<delete id="deleteNotice" parameterType="com.yomul.vo.NoticeVO">
		DELETE FROM YOMUL_NOTICES
		WHERE
			  NO=#{no}
	</delete>
</mapper>